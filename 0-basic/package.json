{
  "name": "doro-basic",
  "scripts": {
    "clean": "rimraf dist && mkdirp dist",
    "build": "yarn clean && cpx 'src/*.{html,js,css}' dist",
    "serve": "http-server dist -p 5000"
  },
  "devDependencies": {
    "cpx": "^1.5.0",
    "http-server": "^14.1.1",
    "mkdirp": "^3.0.1",
    "rimraf": "^6.0.1"
  }
}
