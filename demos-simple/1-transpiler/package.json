{
  "name": "doro-transpiler",
  "scripts": {
    "build": "rimraf dist && babel src -d dist && cpx 'src/*.{html,css}' dist"
  },
  "devDependencies": {
    "@babel/cli": "^7.28.3",
    "@babel/core": "^7.28.4",
    "@babel/preset-env": "^7.28.3",
    "cpx": "^1.5.0",
    "rimraf": "^6.0.1"
  }
}
